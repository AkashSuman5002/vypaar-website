PORT=5000
MONGODB_URI=mongodb://localhost:27017/vyapar?replicaSet=rs0JWT_SECRET=81df575862d6258771c5967f9c44e3e88b5b1b8b4579e1c425d0268e87a3bc748662e3830b8674a8c13949a08c920aa3
JWT_EXPIRE=30d
# This MongoDB is a standalone server (not a replica set), which does not support
# multi-document transactions. Allow non-atomic best-effort writes so the app works.
# For a production replica set / Atlas, remove this so writes are fully atomic.
# ALLOW_NON_TRANSACTIONAL=true